import { defineConfig } from "vite";

export default defineConfig({
  // Your custom Vite configuration goes here
  // For example, to configure base path or plugins
  base: "./", // This is useful if you're deploying to a subdirectory
});
